{
  "name": "PRISM-Transformer",
  "user": "ubuntu",
  "install": "pip install --upgrade pip && pip install torch --index-url https://download.pytorch.org/whl/cpu && pip install -r requirements.txt"
}
